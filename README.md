# Payment Link Server

## Quick Start

```bash
cd server
npm install
npm run dev
```

Server will start at `http://localhost:4021`.

## Scripts

- `npm run dev` — Start the development server
- `npm run build` — Compile TypeScript
- `npm test` — Run tests
